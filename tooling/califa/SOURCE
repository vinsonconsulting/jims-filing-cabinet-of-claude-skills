Vendored from: vinsonconsulting/califa-cards
Commit:        81c4bd56ddd322841652dce215408be8e11e5239  (v0.7.1)
Tag:           v0.7.1
Vendored on:   2026-06-23
License:       Apache-2.0 (see ./LICENSE)

Pin note: 81c4bd5 is the v0.7.1 release (merge of PR #11) — a bump-and-tag over
v0.7.0 that ships the upstreamed README content-hash exclusion. hashing.py now
keeps "README.md" in EXCLUDE_NAMES upstream (califa-cards 6d6e27c / PR #9), so
the cabinet's former local hashing.py patch is RETIRED: this vendored copy is
byte-identical to the tag with no cabinet-local deviation. Net hashing behaviour
is unchanged from the prior v0.7.0 vendor (README was already excluded there via
the local patch), so no card content_hash moves. v0.7.1 carries no functional
code change vs v0.7.0 beyond the exclusion plus a README lineage-section cleanup.

This is a pinned copy of Califa's `schema/` + `skillcard/` packages (including
the `skillcard.harness` subpackage, dormant here — `make check` never runs
`skillcard eval`), plus the Jinja template and pyproject, so the public
cabinet's `make check` can validate and gate skill cards without a private-repo
CI secret.

Install:  python3 -m pip install -e "tooling/califa[scan]"

v2 exit: replace this vendored copy with a pinned dependency once Califa
publishes a tagged release / package.
