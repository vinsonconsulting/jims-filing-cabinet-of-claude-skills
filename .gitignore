.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
*.bak
.idea/
.vscode/
.obsidian/

# SkillSpector artifacts regenerated by `make check`: report.sarif is the per-card
# SARIF (staged surface, uploaded to the Security tab in CI); the fresh gate-input
# scan and the drift-build dirs live under .skillcheck/. The per-skill scan.json is
# a FROZEN, COMMITTED build input (refresh it with `make scan`) and is NOT ignored.
report.sarif
skillspector.sarif
.skillcheck/

# Editable-install build artifact from `pip install -e tooling/califa`
tooling/califa/build/
